console.log("hello world ")

let gameName= "spider-men 2 "

// gameName = "god of war ragnarok"
console.log(gameName)

// this is the case of js where variable let can be change when we write the diff  word but if we use the const variable then it will not change 

const username = "no way home "
// we can't use the username again  because it appiers with the const variable
console.log(username);
